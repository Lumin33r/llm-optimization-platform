# =============================================================================
# DO NOT apply this file directly — the ${HF_TOKEN} placeholder is NOT
# auto-substituted. Instead, create the secret via kubectl:
#
#   export HF_TOKEN="hf_your-token-here"
#   kubectl create secret generic hf-token \
#     --from-literal=HUGGING_FACE_HUB_TOKEN="$HF_TOKEN" \
#     -n llm-baseline
#
# This template is retained for reference and for use with sealed-secrets
# or external-secrets-operator in production.
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: hf-token
  namespace: llm-baseline
type: Opaque
stringData:
  HUGGING_FACE_HUB_TOKEN: "${HF_TOKEN}" # Inject via sealed-secrets or external-secrets
