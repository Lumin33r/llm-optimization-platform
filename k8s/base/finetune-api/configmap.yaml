apiVersion: v1
kind: ConfigMap
metadata:
  name: finetune-config
  namespace: finetune
data:
  LOG_LEVEL: "DEBUG"
  AWS_REGION: "us-west-2"
  SAGEMAKER_ENDPOINT_NAME: ""
  VLLM_BASE_URL: "http://mistral-7b-lora.llm-baseline.svc.cluster.local:8000"
  OTEL_EXPORTER_OTLP_ENDPOINT: "http://otel-collector.observability.svc.cluster.local:4317"
  OTEL_SERVICE_NAME: "finetune-api"

  # SageMaker timeout configuration
  SAGEMAKER_TIMEOUT_MS: "60000"

  # Failure handling mode
  ENABLE_FALLBACK: "false"
